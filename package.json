{
  "name": "eda-browser-extension",
  "version": "1.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && node build.mjs --firefox --chromium",
    "build:firefox": "rm -rf dist && node build.mjs --firefox",
    "build:chromium": "rm -rf dist && node build.mjs --chromium",
    "dev:chromium:env": "npm run build:chromium && node dev/launch-chromium-with-extension.mjs",
    "dev:chromium:env:no-build": "node dev/launch-chromium-with-extension.mjs",
    "lint": "oxlint . --deny-warnings",
    "typecheck": "tsc --noEmit",
    "package": "npm run build && web-ext lint --source-dir dist/firefox && web-ext build --source-dir dist/firefox --overwrite-dest && web-ext build --source-dir dist/chromium --overwrite-dest",
    "package:firefox": "npm run build:firefox && web-ext lint --source-dir dist/firefox && web-ext sign --source-dir dist/firefox --channel unlisted && mv web-ext-artifacts/*.xpi web-ext-artifacts/eda_browser_extension.xpi",
    "package:chromium": "npm run build:chromium && web-ext build --source-dir dist/chromium --overwrite-dest",
    "package:crx": "npm run build:chromium && crx3 -o web-ext-artifacts/eda_browser_extension.crx -p key.pem dist/chromium",
    "dev": "npm run build:firefox && web-ext run --source-dir dist/firefox",
    "clean": "rm -rf dist web-ext-artifacts"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@fontsource/roboto": "^5.2.10",
    "@mui/icons-material": "^7.3.9",
    "@mui/material": "^7.3.9",
    "react": "^19.2.4",
    "react-dom": "^19.2.4"
  },
  "devDependencies": {
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "crx3": "^2.0.0",
    "esbuild": "^0.27.3",
    "oxlint": "^1.52.0",
    "playwright": "^1.58.2",
    "typescript": "^5.9.3",
    "web-ext": "^9.4.0"
  }
}